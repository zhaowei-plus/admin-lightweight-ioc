import React from 'react'
import { Frame } from '@/components'

const FormCenter = () => {
  return (
    <Frame
      title="表单中心"
      src={`${window.location.origin}/admin/#/send-form/list`}
    />
  )
}

export default FormCenter
