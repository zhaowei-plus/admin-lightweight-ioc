import React from 'react'
import { Frame } from '@/components'

const FormEngine = () => {
  return (
    <Frame
      title="表单引擎"
      src={`${window.location.origin}/admin/#/baas-form-engine/list`}
    />
  )
}

export default FormEngine
