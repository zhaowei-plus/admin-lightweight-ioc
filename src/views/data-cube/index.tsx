import React from 'react'
import { Frame } from '@/components'

const DataCube = () => {
  return (
    <Frame
      title="数据魔方"
      src={`${window.location.origin}/admin/#/data-cube`}
    />
  )
}

export default DataCube
